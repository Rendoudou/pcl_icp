1. pc_icp.cpp 是主文件，有主函数。包含icp配准和点云拼接及输出拼接结果。

2. output_appended.pcd 拼接结果保存的结果。

3. file 所有从homework_3.bag文件中解压出来部分的pcd存储在那里。

4. used_file 使用到的pcd文件存储在那里。

5. build 可执行文件所在的位置。

6. r_v_filter.cpp r_v_filter.h 滤波器有关内容，未使用。
